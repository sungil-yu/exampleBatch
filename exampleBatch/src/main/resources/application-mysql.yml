spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/batchdb?serverTimezone=UTC&characterEncoding=UTF-8&rewriteBatchedStatements=true
      username: root
      password: q1w2e3r4
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
  batch:
    jdbc:
      initialize-schema: never

