spring:
  batch:
    job:
      names: ${job.name:NONE}
    jdbc:
      initialize-schema:
  datasource:
    driver-class-name: org.h2.Driver
  sql:
    init:
logging:
  level:
    org.springframework.batch.item.database.JdbcPagingItemReader: DEBUG
